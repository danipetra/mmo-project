{
  "name": "mmo-project",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n server,client,dashboard -c blue,green,magenta \"npm run dev -w server\" \"npm run dev -w game-client\" \"npm run dev -w admin-dashboard\"",
    "dev:server": "npm run dev -w server",
    "dev:client": "npm run dev -w game-client",
    "dev:dashboard": "npm run dev -w admin-dashboard"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
